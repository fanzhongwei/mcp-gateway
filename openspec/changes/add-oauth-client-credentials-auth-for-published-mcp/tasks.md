## 1. 配置与双认证骨架

- [x] 1.1 在 `mcp-gateway-server` 中新增双认证配置项（认证模式、优先级、回退策略）与内置 OAuth 配置项（issuer、audience、签名密钥、客户端凭据）。
- [x] 1.2 增加配置加载与启动时参数校验，确保关键配置缺失时可被快速识别。
- [x] 1.3 设计并落地统一认证组件接口（认证路由、token 解析、声明校验）与授权服务接口（凭据校验、token 签发、JWKS 发布），保持 controller 简洁、复杂逻辑下沉 service。

## 2. 核心鉴权实现

- [x] 2.1 实现内置 OAuth token 端点（`/oauth/token`）的 client_credentials 流程与客户端凭据校验。
- [x] 2.2 实现内置 JWKS 端点（`/oauth/jwks`）与签名密钥发布。
- [x] 2.3 在 MCP 发布入口接入统一 Bearer Token 鉴权拦截链，实现旧 Bearer 与 OAuth Bearer 的分支路由。
- [x] 2.4 实现基于内置密钥体系的 OAuth JWT 验签与关键声明校验（至少 `exp`、`iss`、`aud`）。
- [x] 2.5 实现认证优先级与回退策略（如 OAuth 优先失败后回退旧 Bearer）。
- [x] 2.6 为认证失败定义统一且可区分的错误语义与响应映射，并标记命中认证分支。

## 3. MCP 能力声明与兼容性

- [x] 3.1 在 MCP 初始化能力响应中声明 `io.modelcontextprotocol/oauth-client-credentials` 扩展。
- [ ] 3.2 验证“仅旧 Bearer / 仅 OAuth / 双认证并行”三种模式下的兼容行为，确保不影响原有业务逻辑。

## 4. 测试与可观测性

- [x] 4.1 增加 token 端点测试：合法凭据签发成功、非法凭据拒绝、过期时间与声明正确。
- [x] 4.2 增加认证主路径测试：旧 Bearer 合法放行、OAuth 合法放行、缺失 token 拒绝、无效签名拒绝。
- [x] 4.3 增加回退策略测试：首选分支失败后回退成功/失败路径。
- [x] 4.4 增加配置与异常场景测试：配置缺失、密钥异常、过期 token 等。
- [x] 4.5 补充结构化日志字段与排障信息，验证鉴权失败可观测性。

## 5. 发布与回滚准备

- [ ] 5.1 编写联调检查清单（Dify 接入、token 获取、服务级边界划分）并在测试环境验证。
- [x] 5.2 制定灰度发布与回滚步骤，确保异常时可通过配置快速关闭 OAuth 鉴权。
